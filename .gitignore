# Secrets
.env
.env.local

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Per-agent scenario artifacts — version these in your fork after agent-analysis.
# The framework ships *.example.txt templates; copy and populate locally.
scenarios.txt
regressions.txt

# Local working notes + per-agent scratchpads (not part of the framework)
# docs/ holds session artifacts (learnings, results, recap, deferred).
# .gitkeep tracks the directory; content is gitignored.
docs/*
!docs/.gitkeep
_local/

# Session state shared across edd:* sub-skills
.edd/
